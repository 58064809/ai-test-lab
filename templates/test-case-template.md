# 测试用例模板

默认表格格式如下：

| 标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| 示例标题 | P1 | 示例前置条件 | 1. 第一步；2. 第二步 | 1. 预期一；2. 预期二 |

## 填写规则

- 优先级使用 P0、P1、P2、P3。
- 不默认增加“用例类型”列。
- 不强制一个步骤对应一个预期。
- 缺失业务规则时标注“待确认”。
- 不编造页面、字段、状态、接口或数据。